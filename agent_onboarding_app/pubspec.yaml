name: agent_onboarding
description: Agent onboarding for Grahvarta
publish_to: none
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  image_picker: ^1.0.7
  http: ^1.1.2
  intl: ^0.19.0
  shared_preferences: ^2.2.2
  flutter_secure_storage: ^9.2.2
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
